---
interface MatrixRow {
  expected: string;
  values: Record<string, number>;
}

interface Props {
  title: string;
  labels: string[];
  matrix: MatrixRow[];
  labelMap?: Record<string, string>;
}

const { title, labels, matrix, labelMap = {} } = Astro.props;
const maxValue = Math.max(...matrix.flatMap((row) => Object.values(row.values)));
---

<article class="figure-panel">
  <p class="figure-panel-title">{title}</p>
  <div class="matrix-wrap">
    <table class="matrix-table">
      <thead>
        <tr>
          <th>Observed \\ Predicted</th>
          {labels.map((label) => <th>{labelMap[label] ?? label}</th>)}
        </tr>
      </thead>
      <tbody>
        {matrix.map((row) => (
          <tr>
            <th>{labelMap[row.expected] ?? row.expected}</th>
            {labels.map((label) => {
              const value = row.values[label] ?? row.values[`_${label}`] ?? 0;
              const intensity = maxValue === 0 ? 0 : value / maxValue;
              return (
                <td style={`background-color: rgba(26, 58, 92, ${0.08 + intensity * 0.24})`}>
                  {value.toLocaleString("en-US")}
                </td>
              );
            })}
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</article>
