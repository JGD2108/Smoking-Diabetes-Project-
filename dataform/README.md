# Dataform Note

The deployed Dataform repository referenced by the workflow is `health-pipeline-dataform` in the `pipeline-smoking-drinking` project.

This local folder exists so the repo can document the cloud pipeline structure, but the active Dataform workspace is currently managed in GCP rather than stored locally here.
